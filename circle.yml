general:
  artifacts:
    - "target"

test:
  post:
    - lein uberjar
    #- update-android --version
    #- update-android list sdk --all --extended
    - chmod +x build_executable.sh && ./build_executable.sh
    - chmod +x update-android
    - ./update-android list sdk