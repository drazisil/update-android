machine:
  node:
    version: 4.2.6
    
test:
  override:
    - android list sdk --all --extended > $CIRCLE_ARTIFACTS/sdkListing.txt
    - wget https://dl.google.com/android/repository/addons_list-2.xml > $CIRCLE_ARTIFACTS/androidAddonsRepositoryList.txt
