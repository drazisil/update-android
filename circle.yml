experimental:
  picard:
    version: 0.1
    executorType: docker
    containerInfo:
      - image: docker/golang:1.7.1
    stages:
      build:
        workDir: /home/ubuntu/
        steps:
          - type: checkout
            path: /go/src/app
